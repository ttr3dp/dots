#!/bin/sh
case $1 in
    1) sensors | awk '/Tctl/ {print $2}' | sed 's/+//' ;;
    2) setsid "$TERMINAL" -e htop & ;;
    3) notify-send "󰔏 CPU temperature module" "\- Click to open htop" ;;
esac

